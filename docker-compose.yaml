services:
  app:
    build: .
    container_name: catdrive-front
    restart: always
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true
